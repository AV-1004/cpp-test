#include<stdio.h>
int main()
{
    int lowerLimit,UpperLimit;
    printf("Enter any two numbers between which you want to find the prime numbers");
    scanf("%d,%d",&lowerLimit,&upperLimit);

   printf("Factors of %d are: ",lowerLimit);
    
    for (i = 1; i <= num; ++i) {
        if (num % i == 0) {
            printf("%d ", i);
        }
    }
  




    return 0;
}